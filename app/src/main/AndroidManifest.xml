<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.calendar.kotlincource">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".KotlinCodes.KotlinDagger2.TestDagger2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KotlinCodes.KotlinRetrofit.TestRetrofitActivity">

        </activity>
        <activity android:name=".KotlinCodes.KotlinRX.TestRXActivity"></activity>
        <activity android:name=".KotlinCodes.KotlinRecyclerView.KotlinRecyclerViewActivity" />
        <activity android:name=".KotlinCodes.MainActivity" />
        <activity android:name=".KotlinCodes.MVP.TestMVPActivity" />
    </application>

</manifest>